#!/usr/bin/env bash

# TODO: Make a tar archive first.
#       Prompt before deleting.

# Remove install directory (Critical for security!)
rm -rf seopanel/install/

# Or via FTP: Delete the install folder
